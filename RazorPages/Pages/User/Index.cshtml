@page
@model RazorPagesApp.Pages.User.IndexModel
@{
ViewData["Title"] = "User";
}

<style>
    body {
        background-color: #2c3e50;
        color: #ecf0f1;
        font-family: 'Roboto', sans-serif;
    }
    .table {
        color: #ecf0f1;
    }
    .table th, .table td {
        border-color: #34495e;
    }
    .btn-primary, .btn-danger, .btn-secondary {
        background-color: #2980b9;
        border: none;
        color: #ecf0f1;
        padding: 20px;
        font-size: 1.2rem;
        border-radius: 5px;
        margin: 10px 0;
        width: 100%;
        transition: background-color 0.3s ease;
    }
    .btn-primary:hover, .btn-secondary:hover {
        background-color: #3498db;
        color: #fff;
    }
    .btn-danger {
        background-color: #c0392b;
    }
    .btn-danger:hover {
        background-color: #e74c3c;
        color: #fff;
    }
    .text-center {
        text-align: center;
    }
</style>

<h1>Users</h1>

<p>
    <a asp-page="Create" class="btn btn-primary">Create New User</a>
</p>

<table class="table table-striped">
    <thead>
    <tr>
        <th>Id</th>
        <th>Username</th>
        <th>Role</th>
        <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var user in Model.Users)
    {
    <tr>
        <td>@user.Id</td>
        <td>@user.Username</td>
        <td>@user.Role</td>
        <td>
            <a asp-page="Edit" asp-route-id="@user.Id" class="btn btn-warning">Edit</a>
            <a asp-page="Delete" asp-route-id="@user.Id" class="btn btn-danger">Delete</a>
        </td>
    </tr>
    }
    </tbody>
</table>
